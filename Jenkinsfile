pipeline {
    agent {
        docker {
            image 'adoptopenjdk/maven-openjdk8‘
            args '-v /root/.m2:/root/.m2'
        }
    }
    stages {
        stage('Build') {
            steps {
                sh 'mvn -B -DskipTests clean package'
            }
        }
    }
}
